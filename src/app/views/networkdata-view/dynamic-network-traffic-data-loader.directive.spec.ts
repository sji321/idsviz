import { DynamicNetworkTrafficDataLoaderDirective } from './dynamic-network-traffic-data-loader.directive';

describe('DynamicNetworkTrafficDataLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicNetworkTrafficDataLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
